"""Probe models are defined in the test files that use them.

This module exists so ``tests.probes`` is a well-formed Django app; the actual
probe classes live next to their tests under ``app_label = "probes"``.
"""
