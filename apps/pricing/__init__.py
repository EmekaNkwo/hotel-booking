# Pricing (M6)
# Rate plans, overrides, modifiers, and the persisted PriceBreakdown snapshot.
# Price is computed ONLY by PricingService (DR-06) — bookings/reservations ask,
# never compute. Modifiers are data, not hardcoded branches (R3).
