# Guest Profile (M5)
# The guest lifecycle hub — identity resolution, preferences, consent, GDPR erasure.
# Must exist before any booking (SDD S11.9); profile integrity across stays/properties
# is the consistency boundary. Services ask; profile changes never rewrite booking history.
